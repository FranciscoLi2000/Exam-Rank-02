#ifndef DO_OP_H
#define DO_OP_H

# include <stdio.h>
# include <stdlib.h>
# include <unistd.h>

int	sum(int x, int y);
int	sub(int x, int y);
int	multi(int x, int y);
int	divide(int x, int y);
int	mod(int x, int y);

#endif
